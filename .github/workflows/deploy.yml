name: 🚀 Deploy to VPS

on:
  push:
    branches:
      - main  # ВАЖНО: деплой будет запускаться только из main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: �� Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            cd /home/alex/myproject/lending2_backend
            bash deploy.sh

